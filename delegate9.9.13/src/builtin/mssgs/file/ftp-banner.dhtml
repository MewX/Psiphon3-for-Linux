${host.clif} PROXY-FTP server (DeleGate/${ver}) ready.
  @ @
 ( - ) { ${verdate} }
${copyright}
WWW: ${homepage}
--
You can connect to a SERVER by `user' command:
   ftp> user username@SERVER
or by `cd' command (after logged in as an anonymous user):
   ftp> cd //SERVER
Cache is enabled by default and can be disabled by `cd .' (toggle)
This (proxy) service is maintained by '${ADMIN}'
${include.silent:ftp-banner-postfix.dhtml}\
